# Copy this file to .env.local (gitignored) and fill in real values.
# Never put real credentials in the shared .env — that file is committed.
USERNAME=your_test_user@example.com
PASSWORD=your_test_password
